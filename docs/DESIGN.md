# DESIGN.md

## 색상

| 역할 | 값 |
|------|-----|
| 프라이머리 | #2563EB |
| 위험 | #DC2626 |
| 성공 | #16A34A |
| 배경 | #FFFFFF |
| 표면 | #F9FAFB |
| 텍스트 기본 | #111827 |
| 텍스트 보조 | #6B7280 |
| 보더 | #E5E7EB |

Tailwind 매핑: `primary`, `danger`, `success` 커스텀 컬러 등록됨 (`tailwind.config.js`).

## 타이포그래피

| 역할 | 크기 | 굵기 |
|------|------|------|
| 제목 1 | 2rem | 700 |
| 제목 2 | 1.5rem | 600 |
| 본문 | 1rem | 400 |
| 보조 | 0.875rem | 400 |

## 간격 (4px 스케일)
4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 → Tailwind `p-1, p-2, p-3, p-4, p-6, p-8, p-12, p-16`

## 보더 반경
기본 8px (`rounded-lg`) / 버튼 6px (`rounded-md`) / 카드 12px (`rounded-xl`)

## 반응형
모바일 ~767px (`max-md:`) / 태블릿 768~1023px (`md:`) / 데스크탑 1024px~ (`lg:`)

## 터치 타겟
최소 44×44px (`min-w-11 min-h-11`)
